# ==========================================================
# forty-scan - 개발/학습용 의존성
#
# 팀원들은 이 파일로 설치하세요:
#   pip install -r requirements-dev.txt
#
# 위 한 줄(-r)이 프로덕션 목록을 그대로 포함하므로
# 버전 번호가 두 곳에 중복되지 않습니다.
# → dev 와 prod 의 scikit-learn 버전이 갈라질 수 없습니다.
# ==========================================================

-r requirements.txt

# 학습 스크립트 전용 (forty_scan_model.py, forty_scan_해찬.py, forty_scan_종호.py)
# 서빙 경로에서는 import 되지 않아 배포 이미지에서는 제외됩니다.
pandas==3.0.5
