# 영포티 지수 집계 알고리즘 변경

화자 1명의 최종 영포티 지수를 계산하는 방식을 바꿨습니다.
`analyze_conversation()` ([forty_backend.py](forty_backend.py)) 한 곳의 변경이고,
`/analyze`, `/analyze-all` 두 API 모두 이 함수를 쓰므로 함께 바뀝니다.

> **이 문서의 모든 측정치는 특징 26개 모델(절편 `-1.200`, 무증거 기준선 23.1점) 기준입니다.**
> 특징 사전(`forty_scan_features.py`)을 바꾸거나 모델을 재학습하면
> 아래 숫자들은 다시 재봐야 합니다. 로직 자체는 차원에 의존하지 않아 그대로 동작합니다.

---

## 1. 무엇이 문제였나

기존 방식은 **문장별 점수의 평균**이었습니다.

```
문장1 점수, 문장2 점수, ... 문장n 점수
              ↓ 평균
        최종 영포티 지수
```

문제는 문장 하나의 점수가 0점이나 100점 근처로 쏠려 있다는 것입니다.
예를 들어 `"넵~^^"` 한 줄은 그 자체로 **99.8점**이 나옵니다.

이게 평균이랑 만나면 두 가지 방향으로 틀어집니다.

### 문제 A — 메시지가 적으면 오탐

메시지가 1~3개뿐인 화자는, 우연히 섞인 문장 하나가 평균 전체를 지배합니다.

> 예: 대화 내내 평범하게 말하는 사람이 어쩌다 "넵~^^" 한 마디만 남겼다면
> → 그 사람의 전체 대화 수는 셀 수 없이 많더라도, **분석 대상 문장이 1개**뿐이면
> 평균 = 99.8점 = **확진 영포티**로 단정.

실측: 영포티 발화가 10%밖에 안 되는 평범한 화자도, 문장 1개만 분석하면
**7.2%의 확률로 "확진 영포티"** 판정이 나왔습니다.

### 문제 B — 메시지가 많으면 오히려 희석

특징이 하나도 없는 문장(예: `"응"`, `"네"`, `"알겠어"`)의 점수는
**0점이 아니라 23.1점**입니다. (모델의 절편만 작동한 값)

말이 많은 사람은 영포티 발화를 섞어도 평범한 문장들이 23점 쪽으로
평균을 계속 끌어내립니다. 그래서 진짜 영포티인 사람이 대화를 많이 할수록
오히려 판정이 무뎌지는 반대 현상도 있었습니다.

**두 문제의 원인은 같습니다. 포화된(0/100에 몰린) 확률을 평균 내면
표본 수에 따라 의미가 완전히 달라집니다.**

---

## 2. 어떻게 고쳤나

문장별 **확률(점수)**을 평균 내는 대신, 문장별 **특징 등장 횟수**를 합산해서
"이 화자의 평균적인 문장 한 개"를 만들고, 그걸 모델에 딱 한 번 통과시킵니다.

```
                     Σ (문장별 특징 등장 횟수)
final_logit = 절편 + ──────────────────────────── · 계수
                        메시지 수 + PRIOR_MESSAGE_COUNT

final_score = sigmoid(final_logit) × 100
```

이 방식은 원래 모델(로지스틱 회귀)이 애초에 "횟수 벡터 → 확률"로 학습되었기 때문에
수학적으로 정직합니다. 학습 때와 다른 방식으로 서빙하는 게 아니라,
그냥 "여러 문장을 하나로 뭉친 문장"에 같은 모델을 적용하는 것뿐입니다.

### 핵심 아이디어: 분모에 상수를 더한다 (축소, shrinkage)

분모가 `메시지 수`가 아니라 `메시지 수 + PRIOR_MESSAGE_COUNT` 입니다.

- 메시지가 **적으면** → 분모가 실제 메시지 수보다 훨씬 커져서,
  평균 횟수가 0에 가까워지고 → 점수가 **무증거 기준선(23.1점)** 쪽으로 당겨집니다.
- 메시지가 **많으면** → `PRIOR_MESSAGE_COUNT`가 분모에서 차지하는 비중이 작아져서
  → 축소 효과가 거의 사라지고, 진짜 비율이 드러납니다.

즉 "증거가 부족하면 함부로 단정하지 않는다"는 통계적으로 흔한 방법(베이즈 축소)을
이 도메인에 맞게 적용한 것입니다.

---

## 3. 새로 추가된 상수 3개

([forty_backend.py](forty_backend.py)에 정의)

| 상수 | 값 | 의미 |
|---|---|---|
| `PRIOR_MESSAGE_COUNT` | `8` | 축소 강도. "메시지 8개는 모아야 절반쯤 믿는다"는 뜻. 클수록 표본 적은 화자를 더 세게 기준선으로 당김 |
| `MAX_COUNT_PER_SENTENCE` | `3` | 문장 하나가 합산에 기여할 수 있는 특징 최대 횟수. 아래 4번 참고 |
| `MIN_MESSAGES_FOR_GRADE` | `5` | 이 값 미만이면 점수는 보여주되 등급을 `"표본 부족"`으로 표시 |

`PRIOR_MESSAGE_COUNT`가 클수록 오탐은 줄지만 진짜 영포티도 늦게 잡힙니다.
현재 `k=8`은 실제 데이터로 시뮬레이션해서 고른 절충값입니다 (아래 5번 표 참고).
나중에 튜닝하고 싶으면 이 상수 하나만 바꾸면 됩니다.

---

## 4. 도배 방어 (문장당 횟수 상한)

합산 방식에는 새로운 약점이 하나 생깁니다. **한 문장에 특징을 몰아넣는 도배**입니다.

```
"안녕하세요~~~~~~~~~~ ^^ ^^ ^^ ^^ ^^ 오빠가 말이야~~~~~~"
  → 물결(~) 16회, ^^ 5회
```

기존 평균 방식에서는 문장 점수가 100점에서 잘렸기 때문에 이 문제가 안 보였지만,
횟수를 그냥 더하면 이 한 문장이 다른 9개 평범한 문장을 다 뭉갤 수 있습니다.
(도배 문장 1개 + 평범한 문장 9개 → 상한 없이 합산하면 93.8점)

그래서 합산할 때 문장 하나당 같은 특징은 최대 `MAX_COUNT_PER_SENTENCE`(3)번까지만
셉니다. 실제 데이터에서 한 문장에 같은 특징이 3번 넘게 나오는 경우는 **0.16%뿐**이라
정상적인 대화에는 사실상 영향이 없습니다. (위 도배 예시는 54.2점으로 낮아짐)

이 상한은 **집계할 때만** 적용됩니다. 문장 하나짜리 점수(`analyze_sentence`,
`/analyze-sentence` API)는 원래 횟수를 그대로 씁니다 — 학습 때와 동일해야 하므로.

---

## 5. 판정 보류와 신뢰도

메시지 수가 `MIN_MESSAGES_FOR_GRADE`(5) 미만이면:

- 점수(`final_score`)는 그대로 계산해서 보여줍니다 (축소 덕분에 이미 안전한 값).
- 등급(`final_grade`)만 `"표본 부족"`으로 바뀌고, 코멘트에 몇 개인지 표시합니다.
- 응답에 `reliability`(0~1)와 `is_provisional`(true/false)이 추가로 붙습니다.

```
reliability = 메시지 수 / (메시지 수 + PRIOR_MESSAGE_COUNT)
```

프론트(`static/index.html`)는 `is_provisional`이 true인 화자를 카드 색을 회색으로,
점수 막대를 흐리게 표시해서 "아직 판정이 안 났다"는 걸 시각적으로 구분합니다.

---

## 6. 바뀌기 전 / 후 비교 (실제 모델·데이터로 시뮬레이션)

### 평범한 화자(대화 중 영포티스러운 발화가 10%뿐)의 오탐률

| 분석된 문장 수 | 이전 `≥60` | 이후 `≥60` | 이전 `≥80` | 이후 `≥80` |
|---|---|---|---|---|
| 1개 | 10.8% | **0.0%** | 7.2% | **0.0%** |
| 3개 | 2.6% | **0.0%** | 0.1% | **0.0%** |
| 30개 | 0.0% | 0.0% | 0.0% | 0.0% |

상위 5%에 해당하는 점수(p95)로 봐도 문장 1개일 때 **86.3점 → 29.7점**으로 떨어집니다.

### 진짜 영포티(대화 중 70%가 영포티스러운 발화)의 평균 점수

| 분석된 문장 수 | 이전 | 이후 |
|---|---|---|
| 3개 | 55.2 | 35.9 (표본 부족이라 오히려 낮게 → 의도된 것) |
| 10개 | 54.8 | 49.9 |
| 30개 | 54.8 | **62.1** (희석 문제 해소) |
| 100개 | 54.9 | **69.0** |

문장 3개짜리 진짜 영포티가 이전보다 낮게 나오는 건 버그가 아니라 의도입니다.
문장 3개로는 그 사람이 진짜 영포티인지 판단할 근거가 부족하므로,
`is_provisional=true`로 표시되고 등급도 "표본 부족"이 됩니다.

---

## 7. 앞으로 다시 손볼 만한 부분 (이번엔 건드리지 않음)

- **등급 임계값(20/40/60/80) 재보정.** 새 방식에서는 대화 100개짜리 진짜 영포티도
  ~69.0점이라 "확진 영포티(≥80)"에 거의 못 미칩니다. 실제 카톡 로그를 더 모아서
  분위수 기반으로 임계값을 다시 잡는 게 정확하지만, 이번 변경 범위 밖입니다.
- `forty_backend.parse_kakao_txt` 와 `forty_web.parse_kakao_lines` 두 파서가
  서로 달라서, 여러 줄 메시지가 있는 파일은 `/analyze`와 `/analyze-all`의
  메시지 수가 다르게 잡힐 수 있습니다. (이번 변경과 무관한 기존 이슈)
