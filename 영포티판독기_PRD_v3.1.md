# PRD: 영포티 판독기 (Young-Forty Speech Detector)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v3.0 (서비스 구현 완료 + 집계 알고리즘 개편 반영) |
| 작성일 | 2026-08-12 (v1.1/v1.2: 2026-08-11, v2.0: 2026-08-11, v3.0: 2026-08-12) |
| 프로젝트 기간 | 3일 MVP + 확장 세션 (백엔드 안정화, UX/UI 디자인, 발표자료) |
| 프로젝트 성격 | 개인/과제용 ML 실습 프로젝트 → 시연 가능한 웹 서비스로 확장 |
| 기반 문서 | `영포티판독기_PRD_v2.0.md` (원본 보존, 본 문서는 수정본) |

## v3.0 변경 요약

v2.0(multi-hot encoding 설계)까지는 계획 문서였다면, v3.0은 **실제로 동작하는 서비스의 최종 상태**를 기록한다. v2.0 대비 달라진 점은 다음 다섯 갈래다.

1. **집계 알고리즘 전면 개편 (가장 중요한 변경)**
   문장별 점수를 **평균**내던 v2.0 방식은 표본이 적으면 오탐, 많으면 희석되는 구조적 결함이 있었다. v3.0은 점수가 아니라 **특징 등장 횟수**를 문장별로 합산한 뒤, `(메시지 수 + 8)`로 나누는 **축소추정(shrinkage)** 방식으로 교체했다. 자세한 배경은 `영포티_지수_집계_알고리즘_변경.md` 참고.
2. **특징 사전 26개로 확정 + 오타 보정 레이어 추가**
   v2.0에서 "20~30개, 미정"이었던 특징 수를 **26개(영포티 신호 24개 + 일반 신호 2개)**로 확정했고, 정규식이 놓친 변형 표현을 자모 단위 편집거리로 잡아내는 **오타 보정** 레이어를 추가했다.
3. **서비스 아키텍처 확정**
   FastAPI 단일 프로세스가 REST API와 정적 프론트엔드를 동시에 서빙하는 구조로 확정했다. DB는 사용하지 않는다(완전 무상태).
4. **UX/UI 디자인 확정 (구현은 아직)**
   v2.0에서 "Nice to have"였던 결과 공유·등급 부여를 실제 디자인으로 구체화했고, v2.0에 없던 **"내 대화 / 상대방 대화 / 전체" 3분기 분석 목적 선택** 플로우와 모바일·웹 반응형 화면 세트를 새로 설계했다. **이 디자인은 아직 실제 프론트엔드 코드(`static/index.html`)에는 반영되지 않은 상태다** — 현재 동작 중인 화면은 기존 다크테마 버전이다.
5. **실측 성능 수치 확보**
   "정확도 70% 목표"였던 v2.0의 가설을 실제 학습 결과(Accuracy 0.818, F1 0.749)로 교체했다.

---

## 1. 배경 및 문제 정의

*(v2.0과 동일 — 변경 없음)*

'영포티'(젊은 40대인 척하는 40대)는 세대 간 말투 차이에서 오는 흥미로운 밈이지만, 지금까지의 판별 방법은 다음과 같은 한계가 있었다.

- 지인/동료에게 물어보는 방식 → 주관적이고 관계 부담이 큼
- 유튜브·블로그의 '영포티 특징 정리' 콘텐츠 → 자가 체크리스트 수준, 정량화되지 않음
- 사내 세대차 교육/워크숍 → 비용·시간 소모가 크고 개인 단위 진단 불가

**핵심 문제**: 대화 텍스트(카카오톡 등)를 넣으면, 그 안에 영포티적 말투 특징이 몇 개 포함되어 있는지 객관적·정량적으로 세어주는 도구가 없다.

## 2. 목표

- 카카오톡 대화(.txt) 또는 문장 하나를 넣으면 영포티 지수(점수)를 계산하고, 어떤 특징이 점수에 기여했는지 함께 보여주는 **웹 서비스**를 만든다. *(v2.0: "서비스"라는 표현만 있었으나, v3.0은 실제로 브라우저에서 접근 가능한 웹 서비스로 구현됨)*
- **학습 목표**: multi-hot encoding을 직접 구현하고, **여러 문장의 결과를 하나의 점수로 정직하게 합산하는 방법**(축소추정)까지 이해한다. — v3.0에서 목표 범위가 "벡터화"에서 "집계"까지 확장됨.
- 3일 MVP는 "동작하는 파이프라인 완주"를 최우선으로 했고, 확장 세션에서는 **실제 사용 가능한 수준의 정확도와 UX**를 목표로 다듬었다.

## 3. 타겟 사용자

| 사용자 | 니즈 | 대응 플로우 (v3.0 신규) |
|---|---|---|
| 영포티라고 지적당하고 싶지 않은 사람 (자가 진단형) | 자신의 말투를 객관적으로 점검 | "내 대화 분석" 버튼 → 본인 식별 → 결과 |
| 영포티를 당한 사람 / 회피하고 싶은 사람 (피해자형) | 재미있는 테스트로 공감대 형성, 상대 성향 사전 파악 | "상대방 대화 분석" 버튼 → 본인 식별(역산) → 결과 |
| 소개팅·모임 전 상대 성향이 궁금한 사람 (탐색형) | 프로필/게시글 기반으로 빠르게 판단 | "문장 하나로" 즉시 판독 |
| *(신규)* 단톡방 전체가 궁금한 사람 | 참여자 전원의 지수를 한 번에 비교 | "전체 다 보기" → 순위 리스트 |

> v2.0에서는 "카톡 대화 업로드"라는 단일 진입만 있었지만, v3.0은 **파일 업로드**와 **문장 직접 입력** 두 경로로 나뉘고, 파일 업로드 시에도 화자 수(1:1 vs 단체방)에 따라 버튼 구성이 달라지도록 설계했다.

## 4. 범위 정의 (v3.0 기준 재정리)

### 4.1 완료 (Done)
1. 카톡 대화 텍스트(.txt) 업로드 및 파싱 — 완료. 단, `forty_backend.parse_kakao_txt`와 `forty_web.parse_kakao_lines` 두 파서가 별도로 존재 (9.3 알려진 이슈 참고)
2. 영포티 특징 사전 구축 — 완료. **26개 확정** (5.2 참고)
3. Multi-hot 벡터 변환 — 완료. 오타 보정 레이어까지 추가 완료
4. 로지스틱 회귀 등 모델 3종 학습·비교 — 완료. 최종 Logistic Regression 채택 (5.3 참고)
5. 영포티 지수 산출 및 기여 특징 출력 — 완료
6. 지수 등급 부여 — 완료. 5단계 등급 확정 (진성 MZ / 정상 범주 / 영포티 경계 / MZ 코스프레 40대 / 확진 영포티)
7. **(v3.0 신규)** 문장별 점수 평균의 오탐/희석 문제 해결 — 축소추정 집계로 개편 완료
8. **(v3.0 신규)** FastAPI 기반 REST API 5종 + 정적 프론트엔드 서빙 완료
9. **(v3.0 신규)** 화자 1명 지정 분석(`/analyze`), 전체 참여자 일괄 분석(`/analyze-all`), 문장 단건 판독(`/analyze-sentence`) 3가지 API 모두 구현

### 4.2 설계 완료 · 구현 대기 (v3.0 기준 Nice to have)
1. **모바일·웹 반응형 UI/UX 디자인** — 랜딩 → 분석 방식 선택 → 목적 선택 → 본인 식별 → 로딩 → 결과 → 공유까지 전 화면 와이어프레임과 브랜드 아이덴티티(크림/그린/옐로, 스티커형 타이포)를 확정. **단, 실제 프론트엔드 코드(`static/index.html`)에는 아직 반영되지 않았다.** 현재 서버가 서빙 중인 화면은 기존 다크테마 버전 그대로다.
2. 결과 공유 카드 — 디자인상 버튼(카카오톡/인스타그램/페이스북/링크 복사)만 확정, **실제 공유 API 연동은 미구현**
3. "이미지로 저장" 버튼 — 디자인만 확정, 실제 캡처/저장 로직 미구현
4. 로딩 화면 재치있는 문구 — 자리와 로직은 완성, **최종 문구 20종 확정 대기**
5. 등급 임계값(20/40/60/80) 재보정 — 집계 알고리즘이 바뀌면서 새 기준의 점수 분포가 달라짐 (9.1 참고)

### 4.3 Out of Scope (v2.0과 동일하게 유지)
- `CountVectorizer`/TF-IDF 등 sklearn 자동 벡터화
- 임베딩 기반 모델(문맥 이해), LLM 파인튜닝
- 다국어 지원, 실시간 대화 분석, 로그인/회원 시스템, 광고 수익화
- 대화 패턴(응답 속도, 대화 주도권 등) 기반 '호의 판단' 기능
- DB 도입 (현재 무상태 구조가 요구사항을 충분히 만족)

## 5. 핵심 기능 상세

### 5.1 데이터 준비 (최종 확정)

| 항목 | v2.0 (계획) | v3.0 (실측) |
|---|---|---|
| 원시 데이터 | 약 2,000개 목표 | **2,436건** (실제 대화 + ChatGPT 생성 2,151건 + 존댓말 보강 285건) |
| 정제 후 | - | **2,383건** (결측·중복 53건 제거) |
| 라벨 비율 | - | 일반(0) 1,355건(56.9%) · 영포티(1) 1,028건(43.1%) |

**(v3.0 신규) 존댓말 보강 데이터셋**: 초기 데이터는 label=0(일반)이 대부분 반말 채팅이라 "존댓말 = 영포티"로 잘못 학습될 위험이 있었다. 영포티가 아닌 존댓말(업무 공지, 문의 등) 285건을 label=0으로 추가해 이 편향을 바로잡았다.

### 5.2 특징 사전 (26개로 확정)

v2.0에서 "20~30개, 브레인스토밍 예정"이었던 항목을 다음과 같이 확정했다.

| 구분 | 개수 | 예시 |
|---|---|---|
| 영포티 신호 — 표기 습관 | 8개 | 웃음_ㅎㅎ, 이모티콘_^^, 물결_남용, 느낌표, 말줄임표, 존댓말_물결조합, 쉼표_남용, 이모지_특수기호 |
| 영포티 신호 — 어휘 | 8개 | 오빠_호칭, 외모_칭찬, 인연_운명, 인생선배_훈수, 라떼_나때, 요즘애들, 젊음_강조, 옛날_유행어, 감탄사_에혀 |
| 영포티 신호 — 문장 습관 | 8개 | 격식체_습니다, 자기지칭, 상대지칭, 조언_해야지, 인생_담론, 확인_의문, 존칭_님씨 |
| 일반(젊은 세대) 신호 | 2개 | 초성체, 신조어_줄임말 (로지스틱 회귀에서 음(-)의 계수로 지수를 낮춤) |

**(v3.0 신규) 오타 보정 레이어**: 정규식이 놓친 변형 표현("아름따우셔서" 등)을 자모 단위 편집거리로 2차 검출한다. `max(정규식 매칭 횟수, 오타보정 매칭 횟수)`로 집계해 이중 계수를 방지한다.

### 5.3 Multi-hot 벡터화 및 모델링 (결과 확정)

v2.0의 설계 개념(등장 횟수를 값으로 쓰는 벡터화)은 그대로 유지하고, **실제 학습 결과**를 아래와 같이 확정했다.

| 모델 | Accuracy | F1 | 비고 |
|---|---|---|---|
| **Logistic Regression** | **0.818** | **0.749** | ★ 최종 채택 — 계수로 기여 특징 설명 가능 |
| Decision Tree (max_depth=6) | 0.797 | 0.710 | |
| Random Forest (n_estimators=200) | 0.818 | 0.752 | Accuracy는 동률이나 해석 불가로 미채택 |

최종 모델 세부 성능 (검증 477건, 8:2 stratified split):

| 클래스 | Precision | Recall | F1 | Support |
|---|---|---|---|---|
| 일반 (0) | 0.774 | 0.959 | 0.857 | 271 |
| 영포티 (1) | 0.922 | 0.631 | 0.749 | 206 |

일반 문장은 recall 0.959로 놓치지 않고, 영포티 문장은 precision 0.922로 오탐이 적다.

### 5.4 결과 집계 — 축소추정(Shrinkage) *(v3.0 전면 개편)*

v2.0에는 없던 섹션이다. **v2.0/기존 구현에서는 화자의 최종 점수를 "문장별 점수의 산술평균"으로 냈으나, 다음 두 문제가 발견되어 전면 교체했다.**

**문제**
- 문장 단위 확률은 0/100 양끝에 몰려 있다 (예: "넵~^^" 한 줄 = 99.8점).
- 표본이 **적으면** 우연히 섞인 문장 하나가 전체를 지배해 오탐 (실측: 평범한 화자도 문장 1개 분석 시 7.2% 확률로 "확진 영포티" 오판정).
- 표본이 **많으면** 특징 없는 문장(23.1점, baseline)이 평균을 계속 끌어내려 진짜 영포티도 무뎌지는 반대 현상.

**해결**: 문장별 점수 대신 **특징 등장 횟수**를 합산하고, `(메시지 수 + PRIOR_MESSAGE_COUNT)`로 나눈 뒤 모델에 **한 번만** 통과시킨다.

```
total   = Σ min(문장별 특징 횟수, MAX_COUNT_PER_SENTENCE)   # 도배 방어
average = total / (메시지 수 + PRIOR_MESSAGE_COUNT)          # 축소추정
logit   = 절편 + average · 계수
score   = sigmoid(logit) × 100
```

| 상수 | 값 | 의미 |
|---|---|---|
| `PRIOR_MESSAGE_COUNT` | 8 | 축소 강도. 메시지가 적을수록 점수를 기준선(23.1점)으로 당김 |
| `MAX_COUNT_PER_SENTENCE` | 3 | 문장 하나가 기여할 수 있는 특징 최대 횟수 (도배 방어) |
| `MIN_MESSAGES_FOR_GRADE` | 5 | 이 미만이면 등급을 "표본 부족"으로 보류, `reliability`(0~1)·`is_provisional` 필드 반환 |

**개선 효과 (실측, 영포티 발화 10%인 평범한 화자 기준)**

| 분석 문장 수 | 이전 오탐률(≥60점) | 이후 오탐률(≥60점) |
|---|---|---|
| 1개 | 10.8% | **0.0%** |
| 3개 | 2.6% | **0.0%** |
| 30개 | 0.0% | 0.0% |

문장 1개일 때 상위 5%(p95) 점수도 86.3점 → 29.7점으로 낮아졌다. 자세한 시뮬레이션은 `영포티_지수_집계_알고리즘_변경.md` 참고.

### 5.5 서비스 아키텍처 *(v3.0 신규 섹션)*

- **단일 FastAPI 프로세스**가 REST API와 정적 프론트엔드를 동시에 서빙한다. 프론트/백엔드를 별도 서버로 분리하지 않아 3일 일정 내 배포·CORS 복잡도를 최소화했다.
- **DB 미사용(완전 무상태)**: 요청마다 업로드된 TXT를 그 자리에서 파싱·분석해 응답한다. 학습된 모델(.pkl)만 서버 시작 시 1회 로드해 재사용한다. 대화 내용은 분석 후 즉시 폐기되어 서버에 남지 않는다.

**API 명세**

| Method | Endpoint | 설명 |
|---|---|---|
| GET | `/` | 서버 상태 확인용 JSON |
| POST | `/speakers` | 카톡 TXT 업로드 → 등장 화자 목록 추출 |
| POST | `/analyze` | 화자 1명 지정 → 해당 화자 메시지만 분석 |
| POST | `/analyze-sentence` | 문장 1개 즉시 판독 (파일 업로드 없이) |
| POST | `/analyze-all` | 전체 참여자 일괄 분석 + 근거 특징·대표 문장 |
| GET | `/ui/` | 정적 프론트엔드(`index.html`) 서빙 (`StaticFiles` mount) |

### 5.6 UX/UI 디자인 (설계 완료, 프론트엔드 구현 전) *(v3.0 신규 섹션)*

v2.0에서 "Nice to have"로만 언급되던 프론트엔드를 모바일·웹 반응형으로 **설계**했다. 이 섹션은 어디까지나 **디자인 산출물(와이어프레임 HTML)** 기준이며, 아래 내용은 실제 서비스 코드(`static/index.html`)에는 아직 반영되지 않았다. 현재 배포된 프론트엔드는 이 설계 이전의 다크테마 버전이 그대로 동작 중이다. 다음 스프린트에서 이 설계를 실제 프론트엔드 코드로 옮기는 작업이 남아 있다 (10. 향후 로드맵 참고).

**분석 진입 플로우**: 랜딩(첫인상) → 분석 방식 선택(파일 업로드 / 문장 직접 입력) → *(파일인 경우)* 분석 목적 선택 → *(필요시)* 본인 식별 → 로딩 → 결과 → 공유

**분석 목적 선택 — 화자 수에 따라 버튼 구성이 달라짐**

| 화자 수 | 버튼 구성 |
|---|---|
| 1:1 대화 (2명) | 내 대화 분석 / 상대방 대화 분석 / 전체(둘 다) 분석 |
| 단체방 (3명+) | 특정 인물 분석 / 전체 분석 (1:1의 "상대방" 개념이 모호하므로 재구성) |

"당신은 누구인가요?"는 단 한 번만 묻고, 그 답으로 "내 대화"와 "상대방 대화" 둘 다를 동시에 결정한다.

**브랜드 아이덴티티**: 배경 크림(`#FEF9F0`), 주 컬러 그린(`#158F69`), 포인트 컬러 옐로(`#FEC926`). 서비스명은 흰 테두리(stroke)를 두른 스티커형 타이포로 표현.

**결과 화면**: 점수는 텍스트 나열 대신 반원형 게이지로 시각화하고, 근거 특징·참여자별 상세는 기본적으로 접어두고(아코디언) 필요할 때만 펼치는 구조로 정보량을 최소화했다.

## 6. 기술 스택 (확정)

| 구분 | 사용 기술 |
|---|---|
| 백엔드 | Python 3.12, **FastAPI**, uvicorn, Pydantic |
| 모델/데이터 | pandas, numpy, scikit-learn (`LogisticRegression`, `DecisionTreeClassifier`, `RandomForestClassifier`), joblib (모델 직렬화) |
| 특징 추출 | `re`(정규식), 자모 분해 기반 오타 보정 (자체 구현) |
| 프론트엔드 | Vanilla HTML/CSS/JS (프레임워크 미사용), `StaticFiles` mount로 FastAPI가 직접 서빙 |
| 데이터 저장 | 없음 (무상태, .pkl 모델 파일만 서버 로컬에 존재) |
| 데이터 생성 | ChatGPT (웹) |

> v2.0 대비 확정된 것: FastAPI(웹 프레임워크), joblib(모델 저장). v2.0에서 검토했던 Streamlit/Google Sheet 프론트 방안은 채택하지 않았다.

## 7. 진행 경과 (Day-by-Day, 실제 기준)

### Day 1 — 데이터 & 베이스라인 ✅
- [x] 실제 대화 + ChatGPT 가상 대화 혼합 데이터셋 확보, 특징 사전 초안

### Day 2 — Multi-hot 벡터화 & 모델 고도화 ✅
- [x] `text_to_multihot()` 구현, 26개 특징으로 확정
- [x] 3개 모델(LogReg/DT/RF) 학습 및 비교, Logistic Regression 최종 채택
- [x] 존댓말 보강 데이터셋 추가로 라벨 편향 보정

### Day 3 — 서비스화 ✅
- [x] FastAPI 백엔드 구현 (`/speakers`, `/analyze`)
- [x] 오타 보정 레이어 추가
- [x] 등급 5단계 확정

### 확장 세션 — 안정화 · UX · 발표 준비 ✅ *(v3.0에서 신규 기록)*
- [x] 집계 알고리즘을 평균 → 축소추정(shrinkage)으로 전면 개편
- [x] `/analyze-sentence`, `/analyze-all` API 추가
- [x] 모바일·웹 반응형 UI 와이어프레임 및 브랜드 디자인 **설계** 완료 (별도 HTML 산출물)
- [x] "나/상대방/전체" 3분기 분석 목적 선택 플로우 설계
- [x] 머신러닝 팀 프로젝트 발표자료(PPTX) 제작
- [ ] **새 UX/UI 디자인을 실제 프론트엔드 코드(`static/index.html`)에 반영** — 아직 착수 전, 현재는 기존 다크테마 화면이 그대로 서빙 중
- [ ] 공유 기능 실제 API 연동 (카카오톡/인스타그램/페이스북)
- [ ] 로딩 화면 문구 20종 최종 확정
- [ ] 등급 임계값 재보정

## 8. 성공 지표 (Success Metrics) — 실측 반영

v2.0의 "정확도 70% 이상이면 양호"라는 가설적 목표를 실제 수치로 교체한다.

| 평가지표 | 목표(v2.0) | 실측(v3.0) |
|---|---|---|
| Accuracy | 70%+ | **81.8%** |
| F1-Score | 명시 없음 | **0.749** |
| 표본 1개일 때 오탐률(≥60점) | 명시 없음 | 10.8% → **0.0%** (축소추정 적용 후) |

파이프라인 완주 여부, 기여 특징의 사람이 읽을 수 있는 출력 여부는 v2.0과 동일하게 충족했다.

## 9. 리스크 및 대응 (갱신)

### 9.1 신규 리스크 (v3.0)

| 리스크 | 대응 방안 |
|---|---|
| 집계 알고리즘 변경으로 기존 등급 임계값(20/40/60/80)이 새 점수 분포와 안 맞을 수 있음 | 실제 카톡 로그를 더 모아 분위수 기반으로 재보정 필요 (미해결, 로드맵으로 이관) |
| `forty_backend.parse_kakao_txt`와 `forty_web.parse_kakao_lines` 두 파서가 서로 달라, 여러 줄 메시지가 있는 파일은 `/analyze`와 `/analyze-all`의 메시지 수가 다르게 잡힐 수 있음 | 두 파서 통일 필요 (미해결, 로드맵으로 이관) |
| 오타 보정이 정규식과 같은 특징을 중복으로 셀 수 있음 | `max(정규식, 오타보정)`으로 이중 계수 방지 완료 |

### 9.2 기존 리스크 (v2.0, 현재 상태)

| 리스크 | v2.0 대응 방안 | v3.0 현황 |
|---|---|---|
| ChatGPT 생성 데이터가 실제 말투와 괴리 | 프롬프트 구체화, 실제 데이터 혼합 | 유지 (2,151건 중 실제 데이터 비중 확보) |
| 특징 키워드 사전 편향 | 다인 브레인스토밍 | 26개로 확정, 존댓말 보강 데이터로 추가 보정 |
| 실제 대화 데이터 개인정보 포함 가능성 | 익명화/마스킹 | 서비스 자체가 무상태라 서버에 데이터가 남지 않음 (구조적으로 해소) |
| 라벨 비율 불균형 | stratify 분할 | 유지 (56.9% : 43.1%, 8:2 stratified 분할 적용) |

## 10. 향후 로드맵 (v3.0 기준 재정리)

**단기 (다음 스프린트)**
- **새 UX/UI 디자인을 실제 프론트엔드 코드로 구현** — 현재 `static/index.html`은 이번 디자인 세션 이전의 다크테마 버전 그대로이며, 별도 HTML 와이어프레임으로만 존재하는 설계를 실제 서비스에 반영하는 작업이 우선순위 1순위
- 등급 임계값(20/40/60/80) 재보정 — 새 집계 방식의 실제 점수 분포 기반
- `forty_backend`/`forty_web` 파서 통일
- 결과 공유 실제 API 연동 (카카오톡 SDK, 이미지 캡처)
- 로딩 문구 20종 확정 및 적용

**장기 (Out of Scope 유지)**
- `CountVectorizer`/임베딩 기반 벡터화 도입으로 사전 밖 표현 반영
- 한국어 문장 임베딩(ko-sroberta, BGE-M3 등) 기반 문맥 판별
- 로그인/회원 시스템, 다국어 지원
- 결과 공유를 통한 바이럴 확산, 광고 수익화

