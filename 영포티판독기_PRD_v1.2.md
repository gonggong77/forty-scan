# PRD: 영포티 판독기 (Young-Forty Speech Detector)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.2 |
| 작성일 | 2026-08-10 (v1.1 수정: 2026-08-11, v1.2 수정: 2026-08-11) |
| 프로젝트 기간 | 3일 (초보자 대상 미니 프로젝트) |
| 프로젝트 성격 | 개인/과제용 ML 실습 프로젝트 |

---

## 1. 배경 및 문제 정의

'영포티'(젊은 40대인 척하는 40대)는 세대 간 말투 차이에서 오는 흥미로운 밈이지만, 지금까지의 판별 방법은 다음과 같은 한계가 있었다.

- 지인/동료에게 물어보는 방식 → 주관적이고 관계 부담이 큼
- 유튜브·블로그의 '영포티 특징 정리' 콘텐츠 → 자가 체크리스트 수준, 정량화되지 않음
- 사내 세대차 교육/워크숍 → 비용·시간 소모가 크고 개인 단위 진단 불가

**핵심 문제**: 대화 텍스트(카카오톡 등)를 넣으면, 그 안에 영포티적 말투 특징이 몇 개 포함되어 있는지 객관적·정량적으로 세어주는 도구가 없다.

## 2. 목표

- 카카오톡 대화(.txt)를 업로드하면 영포티 지수(점수)를 계산하고, 어떤 특징이 점수에 기여했는지 함께 보여주는 서비스를 만든다.
- **학습 목표**: 텍스트를 머신러닝이 이해할 수 있는 숫자 벡터로 바꾸는 3가지 방법(원-핫 인코딩, 벡터화, 임베딩)을 실습하며 차이를 이해한다.
- 3일 안에 "동작하는 결과물"을 완성하는 것을 최우선으로 한다 (완벽한 정확도보다 파이프라인 완주 우선).

## 3. 타겟 사용자

| 사용자 | 니즈 |
|---|---|
| 영포티라고 지적당하고 싶지 않은 사람 | 자신의 말투를 객관적으로 점검 |
| 영포티를 당한 사람 / 회피하고 싶은 사람 | 재미있는 테스트로 공감대 형성, 상대 성향 사전 파악 |
| 소개팅·모임 전 상대 성향이 궁금한 사람 | 프로필/게시글 기반으로 빠르게 판단 |

## 4. 범위 정의 (3일 완주를 위한 우선순위)

### 4.1 MVP (반드시 구현 — Day 1~2)
1. 카톡 대화 텍스트(.txt) 업로드 및 파싱
2. 영포티 특징 사전(키워드/패턴 리스트) 구축
3. 텍스트 → 특징 벡터 변환 (`CountVectorizer(binary=True)` → 등장 횟수/TF-IDF 순으로 고도화)
4. 로지스틱 회귀 모델 학습 (실제 대화 + 챗GPT 생성 가상 데이터 혼합 기반)
5. 영포티 지수(점수) 산출 및 기여 특징 출력

### 4.2 Nice to have (시간 남으면 — Day 3)
6. 지수에 따른 재미있는 이름 부여 ("MZ 코스프레 40대" 등급 등)
7. 영포티가 되지 않기 위한 행동 추천 (간단 규칙 기반)
8. 결과 공유 카드(이미지/링크) 생성

### 4.3 Out of Scope (이번 3일에는 제외)
- 임베딩 기반 모델(방안 3), LLM 파인튜닝 → "향후 확장"으로 로드맵에만 기록
- 다국어 지원, 실시간 대화 분석, 로그인/회원 시스템, 광고 수익화
- 대화 패턴(응답 속도, 대화 주도권 등) 기반 '호의 판단' 기능

## 5. 핵심 기능 상세 (MVP)

### 5.1 데이터 준비
- 챗GPT에게 "영포티 특유의 말투가 강하게 나타나는 대화"와 "일반적인/젊은 세대 대화"를 각각 프롬프트로 생성 요청
- 생성 시 다양성 확보를 위해 상황(직장, 친구, 소개팅 등)을 바꿔가며 여러 번 요청
- CSV로 정리: `text, label(1=영포티, 0=일반)`
- **[진행 완료]** 실제 대화 + 가상(챗GPT 생성) 데이터를 혼합해 **약 2,000개** 샘플 확보 (당초 목표 100~200개 대비 초과 달성)
  - 실제 데이터가 섞여 있으므로 개인정보(이름·연락처 등) 마스킹/제거 여부를 데이터 정리 단계에서 반드시 확인
  - 실제 vs 가상 데이터 비율, 라벨(1/0) 비율이 한쪽으로 치우치지 않았는지 점검 필요

### 5.2 특징 사전 구축
영포티 특징 카테고리 예시 (직접 리서치 + 챗GPT 브레인스토밍으로 **20~30개 특징 확정 — [진행 완료]**):
- 어휘: '라떼는 말이야', '나 때는', '요즘 애들은', '개저씨' 방지용 표현 등
- 이모티콘/표기: ㅋㅋ 대신 ㅎㅎ, 느낌표 과다 사용, 마침표 남용, '~^^', '...', '방가' 등
- 문장 습관: 조언 남발, "~해야지", 옛날 유행어 사용

> **구현 메모**: 확정된 키워드 리스트는 `CountVectorizer(vocabulary=my_features)`처럼 `vocabulary` 파라미터에 고정 리스트로 전달하여 구현한다. `binary=True` 옵션을 주면 "단어 등장 여부(0/1)"만 세는 One-Hot과 동일한 결과를 얻을 수 있어, **정규식으로 별도 One-Hot 함수를 직접 구현할 필요는 없다.** (사전 밖 단어는 Day2 TF-IDF 단계에서 별도로 반영)
> 옵션으로, 단순 등장 횟수 외에 **문장 길이 대비 비율(%)**을 피처로 추가하면 정확도 개선에 도움이 된다.
> 학습 목표(원-핫 인코딩 원리 이해)를 위해, `CountVectorizer(binary=True)` 결과와 정규식 기반 수동 One-Hot 벡터가 동일함을 짧게 비교/검증하는 코드 셀은 학습용으로 남겨둔다.

### 5.3 벡터화 및 모델링
| 단계 | 기법 | 산출물 |
|---|---|---|
| (Day1~2) | `CountVectorizer(vocabulary=키워드리스트, binary=True)`로 이진 벡터화 → 등장 횟수/TF-IDF로 고도화 + LogisticRegression 등 모델 비교 | 해석 가능한 베이스라인 → 사전에 없는 표현까지 반영하는 개선 모델 |
| 향후 | 한국어 문장 임베딩(ko-sroberta 등) | 문맥 기반 고도화 (Out of Scope) |

> Day1과 Day2는 별도 구현이 아니라 **`CountVectorizer` 하나의 옵션 차이**(`binary=True` vs 기본 카운트 vs `TfidfVectorizer`)로 단계적으로 고도화한다.

**모델 후보 (표 데이터에 강한 지도학습 분류 모델 중 선택/비교)**

| 모델 | 특징 | 선택 이유 |
|---|---|---|
| 의사결정나무 (Decision Tree) | "어떤 키워드가 몇 번 이상 나오면 영포티인가"를 룰 형태로 보여줌 | 해석이 가장 쉬움 |
| 랜덤 포레스트 (Random Forest) | 여러 의사결정나무를 결합 | 과적합 방지, 안정적인 성능 |
| 로지스틱 회귀 (Logistic Regression) | 25개 특징에 가중치 부여, 확률(0~100%) 산출 | 가장 기본적인 베이스라인, 계수로 기여도 해석 용이 |

> Day1 베이스라인은 로지스틱 회귀로 고정하고, Day2에서 의사결정나무/랜덤포레스트를 추가로 학습해 성능·해석력을 비교한다.

### 5.4 결과 출력
- 영포티 지수 = 모델 예측 확률(0~100%) 또는 매칭된 특징 개수
- 기여도 상위 특징: 로지스틱 회귀 계수(coefficient)를 이용해 어떤 단어/패턴이 점수를 올렸는지 순서대로 출력

## 6. 기술 스택 제안

- 언어/환경: Python 3.x, Google Colab 또는 Jupyter Notebook (설치 부담 최소화)
- 라이브러리: `pandas`, `re`(정규식), `scikit-learn`(`CountVectorizer`, `TfidfVectorizer`, `LogisticRegression`)
- 데이터 생성: ChatGPT(웹 또는 API)
- (선택) 결과 공유용 프론트: 간단한 Streamlit 앱 또는 Google Sheet

## 7. 3일 일정 (Day-by-Day)

### Day 1 — 데이터 & 베이스라인 ✅ (완료, 2026-08-10)
- [x] 실제 대화 + 챗GPT 가상 대화 혼합, 약 2,000개 샘플 CSV 정리 (`text, label`)
- [x] 영포티 특징 키워드 사전 20~30개 확정
- [x] 전체 기획(PRD) 완료

### Day 2 — 모델 고도화
- [ ] `CountVectorizer(vocabulary=키워드리스트, binary=True)`로 이진 특징 벡터 생성 *(정규식 기반 수동 One-Hot 함수는 별도 작성 불필요 — 학습용 비교 코드만 짧게 남김)*
- [ ] LogisticRegression으로 1차 베이스라인 모델 학습 및 정확도 확인
- [ ] CountVectorizer(vocabulary=키워드리스트, 기본 카운트)/TfidfVectorizer로 벡터화 방식 교체, 동일 데이터로 재학습
- [ ] 의사결정나무 / 랜덤포레스트 / 로지스틱회귀 모델 학습 및 비교
- [ ] `train_test_split(..., stratify=y)`로 학습/검증 데이터 분할 (라벨 비율 유지)
- [ ] `classification_report`로 정확도·정밀도·재현율·F1-Score 4대 지표 확인
- [ ] 두 방식(binary 이진 vs 카운트/TF-IDF) 및 3개 모델 성능/해석력 비교
- [ ] 기여 특징(계수) 출력 기능 구현
- [ ] 새 대화 입력 시 점수 산출하는 함수/노트북 완성

### Day 3 — 마무리 & 발표 준비
- [ ] 지수 등급별 재미 요소(이름/멘트) 추가
- [ ] 결과 시각화(막대그래프 등 간단한 차트)
- [ ] 웹 서비스로 제작 (Streamlit 등으로 대화 입력 → 점수/등급 출력 데모 화면 구성)
- [ ] 발표/보고서용 결과 캡처 및 정리

## 8. 성공 지표 (Success Metrics)

- 정확도(Accuracy) 자체보다 **파이프라인 완주**(데이터 생성 → 벡터화 → 학습 → 예측 → 평가 → 해석)가 1차 성공 기준
- 실제+가상 혼합 데이터 기준 정확도 70% 이상이면 초보자 3일 프로젝트로 양호
- "어떤 특징이 영향을 주었는지"를 사람이 읽을 수 있는 형태로 출력 가능한지
- **분류 4대 핵심 지표**로 모델을 검증 (`classification_report` 활용, 데이터 불균형 대비 F1-Score 필수 확인)

| 평가지표 | 설명 | 영포티 판별에서의 의미 |
| --- | --- | --- |
| 정확도 (Accuracy) | 전체 데이터 중 맞게 예측한 비율 | 전체 카톡 중 영포티/일반을 제대로 맞춘 비율 |
| 정밀도 (Precision) | 영포티라고 **예측한 것 중** 진짜 영포티 비율 | 모델이 "영포티!"라고 한 것 중 오발령이 얼마나 적은지 |
| 재현율 (Recall) | **실제 영포티 중** 모델이 맞춘 비율 | 실제 영포티 텍스트를 얼마나 놓치지 않고 찾아냈는지 |
| F1-Score | 정밀도와 재현율의 조화평균 | 두 지표의 균형을 나타내는 종합 점수 (데이터 불균형 시 필수) |

## 9. 리스크 및 대응

| 리스크 | 대응 방안 |
|---|---|
| 챗GPT 생성 데이터가 실제 말투와 괴리가 있을 수 있음 | 프롬프트에 구체적 상황/예시를 넣어 다양성 확보, 소량 직접 검수 (실제 대화 혼합으로 일부 완화됨) |
| 특징 키워드 사전이 편향될 수 있음 | 최소 3인 이상 브레인스토밍 또는 커뮤니티 자료 참고 |
| 실제 대화 데이터에 개인정보(이름·연락처·회사명 등)가 포함될 수 있음 | 학습 전 익명화/마스킹 처리, 외부 공유·발표 자료에는 실제 원문 노출 금지 |
| 실제/가상 데이터 간 라벨 비율이 불균형할 수 있음 | `train_test_split(..., stratify=y)`로 분할 시 비율 유지, `classification_report`로 클래스별 정밀도·재현율 확인 |

## 10. 향후 로드맵 (Out of Scope, 참고용)

- 실제 카톡 대화 데이터 기반 정량 지수화 고도화
- 한국어 문장 임베딩(ko-sroberta, BGE-M3 등) 도입으로 문맥 기반 판별
- NLP/LLM 파인튜닝을 통한 다른 밈/세대 특징으로 모델 확장
- 공유하기 기능을 통한 바이럴 확산, 광고 수익화

